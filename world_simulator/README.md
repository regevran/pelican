# World simulator

The world simulator produces synthetic `WorldState` data for computation-first development. It publishes a fixed pool and flock for the initial static slice.

Its message interface is declared in `messages.yaml`.
